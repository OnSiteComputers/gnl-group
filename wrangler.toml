# Intentionally left without [assets]/not_found_handling config: Pages (git-connected) projects do not support the [assets] block in wrangler.toml. SPA fallback is handled via public/_redirects instead.
